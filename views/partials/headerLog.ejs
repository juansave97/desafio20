<header>
  <nav class="navbar navbar-expand-md navbar-dark fixed-top bg-dark">
    <div class="container-fluid">

      <a class="navbar-brand" href="#">Coderhouse</a>

      <div class="collapse navbar-collapse" id="navbarCollapse">
        <ul class="navbar-nav me-auto mb-2 mb-md-0">

          <li class="nav-item  <% if (current == `formProductos`) { %> active <% } %> ">
            <a class="nav-link" href="/productos">Productos</a>
          </li>

          <li class="nav-item  <% if (current == `carrito`) { %> active <% } %> ">
            <a class="nav-link" href="/carrito">Carrito</a>
          </li>

          <li class="nav-item  <% if (current == `orders`) { %> active <% } %> ">
            <a class="nav-link" href="/ordenes">Ordenes</a>
          </li>

          <li class="nav-item  <% if (current == `login`) { %> active <% } %> ">
            <a class="nav-link" href="/login">Chat</a>
          </li>

          <li class="nav-item  <% if (current == `logout`) { %> active <% } %> ">
            <a class="nav-link" href="/logout">Salir</a>
          </li>
        </ul>

      </div>
    </div>
  </nav>
</header>